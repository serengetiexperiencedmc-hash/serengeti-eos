# I3.15 — Allowlist Expiry & Audit Export

**Increment:** I3.15  
**Server version:** `0.51.0-i4.11-i3.15`

## Summary

Optional `expiresAt` on transactional allowlist entries; expired entries no longer bypass suppressions. CSV/JSON export for audit.

## API

| Route | Purpose |
| --- | --- |
| POST `/v1/notifications/email/allowlist` | `{ email, note?, expiresAt? }` |
| GET `/v1/notifications/email/allowlist?includeExpired=` | List (active by default) |
| GET `/v1/notifications/email/allowlist/export` | CSV/JSON audit export |

Migration `051_i315_email_allowlist_expiry.sql` adds `expires_at`.

## Health

`increment: I3.15`

## Verification

```bash
npm run test -w @sedmc/api   # i3.15-allowlist-expiry.test.ts
```
