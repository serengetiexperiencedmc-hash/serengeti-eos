# I3.19 — Allowlist dual-control reminder notifications

Live inbox items for SES-noted VIP allowlist rows awaiting second-principal approval.

| Field | Value |
| --- | --- |
| key | `allowlist-ses-dual:{id}` |
| severity | urgent |
| category | approval |
| href | `/commercial/notifications` |
| gate | `notification:dispatch:email` |

Clears automatically on approve / revoke / expiry. Included in the existing email digest as urgent items. Banner: `I3.19`.

## Tests

`apps/api/src/i3.19-allowlist-dual-control-reminder.test.ts`
