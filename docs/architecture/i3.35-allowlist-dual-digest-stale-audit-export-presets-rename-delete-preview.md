# I3.35 — Rename / delete allowlist stale-audit export presets

**Increment:** I3.35  
**Name:** Rename / delete allowlist stale-audit export presets  
**Server version:** `0.99.0-i3.35`  
**Status:** Shipped in Development/Test — commit `e90bbc6`  
**Environment:** Development/Test only  
**Autonomy:** None. Operator-only I3 notification UX. I20 remains at I20.22.

This document remains the implementation contract. Implementation shipped in `e90bbc6`. It does not authorize I3.36, I4.32, I20.23, PG.30, C2+, UAT, Production, or AI-agent work.

## Problem statement

I3.33 added named tenant allowlist stale-audit export presets. I3.34 persists them. Operators can create or upsert by name, but they cannot rename or delete a preset without overwriting filter fields via upsert. I3.34 explicitly deferred “Rename / delete presets”. I20.20 already solved the same gap for recommend-audit presets.

I3.35 closes that I3.34 gap by mirroring I20.20 onto the I3.33/I3.34 allowlist preset store, using existing I0/I1 authorization and ADR-0017 dual-write. It does not invent I20.23 or PG.30.

## Scope

- Rename a tenant allowlist stale-audit export preset by id
- Delete a tenant allowlist stale-audit export preset by id
- Reuse I3.33 name normalization and I3.34 persist/hydrate tables
- Await persist on successful rename; await persist-delete on successful delete when `dbPool` is set
- Status GET continues to echo sanitized `presets` without persisting
- Commercial Notifications: **Rename preset** and **Delete preset** next to **Save preset**
- Map `conflict` → HTTP 409 on notification routes (I20 AI `sendError` already did this; I3 `sendError` now maps `conflict` the same way)

## Non-scope

- I20.23 (undefined / not authorized)
- PG.30 (undefined / not authorized)
- I4.32 DLQ rename/delete
- I20.21 / I20.22 usage audit or last-used preset (no I3 usage tables; do not add them)
- New I20 recommend keys, drafts, or autonomy changes
- LLM providers or vendor SDKs in domain code (ADR-0008 remains proposed)
- Autonomous AI or autonomous apply
- Merge, email send, owner-assign, or approve
- New I2 workflow definitions, instances, tasks, or rules
- New I4 event types or outbox emissions
- Production NATS, production infrastructure, UAT, Production
- C2+ commercial-domain work
- External production integrations
- Closing ADR-0006 / 0012 / 0013
- Changing the running I4.31 implementation or I4 increment string

## Dependencies

| Depends on | Role |
| --- | --- |
| I0 | `authorize()`, RBAC/ABAC, tenant on `principal.tenantId`, audit hash chain (existing) |
| I1 | Session / principal lifecycle; local Dev/Test identity |
| I2 | No new workflow. I3.35 must not add a second rules/workflow engine |
| I3.33 | Preset model, normalize/sanitize, upsert-by-name, apply on export |
| I3.34 | Table `notif_allowlist_dual_digest_stale_audit_export_preset`, upsert persist, hydrate-by-id |
| I20.20 | Behavioural template (errors, persist-on-write, no GET persist) |
| ADR-0017 | Dual-write when `store.dbPool` is set; in-memory remains Dev/Test read SoR |

Does not depend on I20.23, PG.30, C1 Gate, or closing hosting/IdP/secrets ADRs.

## Current implementation baseline

As of API **`0.98.0-i4.31`** (canonical increment I4.31; I3 increment string still **I3.34**):

| Surface | Baseline |
| --- | --- |
| Kernel | `NotifAllowlistDualDigestStaleAuditExportPreset`, `sanitize*`, `normalize*` (trim, collapse spaces, max 80) |
| Store | `notifAllowlistDualDigestStaleAuditExportPresets` |
| Create/upsert | `POST …/allowlist-dual-digest-stale/export/presets` — `notification:dispatch:email`, action `write:allowlist_dual_digest_stale_audit_export_preset` |
| List | `GET …/export/presets` and status `presets` — `notification:read:email_outbox` |
| Apply | `GET …/export?preset=` / `presetId=` |
| Persist | I3.34 `upsertNotifAllowlistDualDigestStaleAuditExportPreset`; hydrate `i334_allowlist_dual_digest_stale_audit_export_preset_hydrate` |
| DDL | Migration `074_i334_…` — PK `id`, unique `(tenant_id, lower(name))` |
| UI | Notifications: Audit preset / Preset name / Save preset |
| I3 route errors | `forbidden` 403, `not_found` 404, else 400 — **no `conflict` → 409 yet** |
| I20.20 | Rename/delete + persist-delete already shipped for recommend presets |

I3.35 adds rename/delete only. It does not add a new preset table.

## Control-plane reuse (mandatory)

I3.35 must use the existing I0 → I1 → I2 → I4 plane:

- **I0:** `authorize({ principal, permission, action })` before mutate; tenant from the authenticated principal
- **I1:** existing login/session; no new identity provider
- **I2:** no `WorkflowDefinition` / instance / rule document for rename or delete
- **I4:** no new catalogue event and no outbox emit for local preset metadata

A second workflow/rules engine is a defect.

## API routes

| Method | Path | Perm | Behaviour |
| --- | --- | --- | --- |
| `POST` | `/v1/notifications/email/allowlist-dual-digest-stale/export/presets/:id/rename` | `notification:dispatch:email` | Rename by id |
| `DELETE` | `/v1/notifications/email/allowlist-dual-digest-stale/export/presets/:id` | `notification:dispatch:email` | Delete by id |

Unauthenticated → `401` `unauthenticated` (existing `principalFromAuthHeader`).

Existing I3.33/I3.34 list/upsert/export/status routes stay. Successful responses include `increment: "I3.35"`.

### Rename success body

```
{ preset, presets, increment: "I3.35" }
```

`preset` and `presets` are sanitized (no `tenantId`, no `createdByPrincipalId`). `preset.id` is unchanged.

### Delete success body

```
{ presets, increment: "I3.35" }
```

## Authorization permissions

| Operation | Permission | `authorize` action |
| --- | --- | --- |
| Rename | `notification:dispatch:email` | `rename:allowlist_dual_digest_stale_audit_export_preset` |
| Delete | `notification:dispatch:email` | `delete:allowlist_dual_digest_stale_audit_export_preset` |
| List / status / export | `notification:read:email_outbox` | unchanged |

Deny → `{ error: "forbidden", reason }` (RBAC `rbac`, inactive principal, or ABAC).  
**403 fixture:** Bob approver (no `notification:dispatch:email`), same as I3.33. Do not use Alice finance for the 403 case (Alice has dispatch).

This is not `ai:write:draft`. I20.20 keeps that permission on the recommend series.

## Tenant-isolation requirements

- Lookup is `tenantId === principal.tenantId` **and** `id`
- A valid id from another tenant is `404` `preset_not_found` (no cross-tenant leak)
- Name clash is evaluated only inside the caller’s tenant
- Unique index `(tenant_id, lower(name))` already enforces this in PG
- Partner tenant `partner-demo` must not see or mutate `sedmc` presets

## Validation rules

Reuse `normalizeNotifAllowlistDualDigestStaleAuditExportPresetName`:

- Trim and collapse internal whitespace
- Empty / whitespace-only → invalid
- Length > 80 → invalid
- Rename payload is `{ name?: string }` only; `action` / `since` / `until` are not accepted on rename

## Error codes

| Condition | `error` | `reason` | HTTP |
| --- | --- | --- | --- |
| Missing/invalid session | `unauthenticated` | — | 401 |
| Missing write permission | `forbidden` | authorize reason | 403 |
| Empty / over-80 name | `invalid_request` | `invalid_name` | 400 |
| Unknown or other-tenant id | `not_found` | `preset_not_found` | 404 |
| Case-insensitive name owned by another preset in the same tenant | `conflict` | `name_taken` | 409 |

Notification `sendError` must treat `conflict` as 409 (copy I20 AI routes). Invalid rename/delete must not persist.

## Rename semantics

1. Authorize write.
2. Normalize `name`. Fail `invalid_name` without store write.
3. Find by `presetId` in the caller tenant. Missing → `preset_not_found`.
4. Find by normalized name in the same tenant. If another row (`clash.id !== existing.id`) → `name_taken`.
5. Same-id case-only rename is allowed (self is not a clash).
6. Keep `id`, `tenantId`, `createdAt`, `createdByPrincipalId`, `action`, `since`, `until`.
7. Set `name` and `updatedAt`.
8. Replace the in-memory row.
9. Await persist (I3.34 upsert-by-id). Unique `(tenant_id, lower(name))` remains valid.
10. Return sanitized preset + tenant preset list.

Rename does not upsert filter fields and does not record a recommend run.

## Delete semantics

1. Authorize write.
2. Find by `presetId` in the caller tenant. Missing → `preset_not_found`.
3. Remove the row from the in-memory collection (hard delete).
4. Await persist-delete by `id` when `dbPool` is set.
5. Return remaining sanitized tenant presets.

After delete:

- List/status omit the row
- Export with that `preset` / `presetId` → `404` `preset_not_found` (existing I3.33 apply rules)
- No I20.21/I20.22 usage or last-preset rows exist on I3; do not add them

## Persistence behaviour

No new migration. Reuse `074_i334_allowlist_dual_digest_stale_audit_export_preset.sql`.

| Operation | PG |
| --- | --- |
| Rename | Existing I3.34 `INSERT … ON CONFLICT (id) DO UPDATE` (name + `updated_at`) |
| Delete | `DELETE FROM notif_allowlist_dual_digest_stale_audit_export_preset WHERE id = $1` (I20.20 pattern) |

Hydrate-on-startup stays I3.34 (`merge` by `id`). Failed dual-write is try/catch, same as I3.34 / I20.20 (“fire-and-forget” after the in-memory write). In-memory remains Dev/Test read SoR (ADR-0017).

## Database-pool behaviour

| `store.dbPool` | Rename | Delete |
| --- | --- | --- |
| unset | Memory only; persist no-ops | Memory only; persist-delete no-ops |
| set | Await upsert | Await delete-by-id |
| query throws | In-memory result still returned; persist error swallowed | Same |

Invalid name, 404, and 409 must not call persist.

## UI behaviour (`/commercial/notifications`)

Add **Rename preset** and **Delete preset** beside **Save preset** (I20.20 AI Drafts layout).

- Rename: enabled when a preset id is selected and the name field is non-empty; POST rename; refresh `presets`; keep selection on the same id; show `Saved`/`Renamed` style status
- Delete: enabled when a preset id is selected; DELETE; refresh `presets`; clear selected id and name
- Selecting a preset still fills action / since / until (I3.33)
- Save preset remains I3.33 upsert-by-name
- Unsigned page: banner/eyebrow only; no credentialed click in shared CRM

## Audit requirements

- Use I0 `authorize()` with distinct rename/delete actions (above)
- Do not add an I2 workflow decision or a new I3.29-style suppression-audit table for rename/delete
- Do not emit I4 domain events for preset rename/delete
- Response bodies stay sanitized

Existing request-completion logs remain the HTTP audit trail.

## Observability requirements

- Existing `request_completed` / correlation id on the new routes
- No new hydrate log (I3.34 hydrate is sufficient)
- Persist-delete failures stay swallowed and must not 500 the client
- Do not change I4.31 DLQ hydrate logs

## Test strategy

`apps/api/src/i3.35-allowlist-dual-digest-stale-audit-export-presets-rename-delete.test.ts`

Model on `i20.20-ai-recommend-stale-audit-export-presets-rename-delete.test.ts`, using allowlist URLs and Bob for 403.

| Case | Expect |
| --- | --- |
| Bob rename / delete | 403 |
| `name: "   "` | 400 `invalid_name`; no persist |
| Unknown id | 404 `preset_not_found` |
| Rename to another preset’s name (case-insensitive) | 409 `name_taken` |
| Rename `"  Last  snoozes "` | 200; same id; name `Last snoozes`; no `tenantId` |
| Delete | 200; remaining names; persist-delete captured when pool mocked |
| Status GET after rename/delete | Echoes `presets`; GET does not persist |
| Export deleted `presetId` | 404 `preset_not_found` |

I3.21–I3.34 increment asserts are `I3.35`. Do **not** bump I4.31 or I20.22.

## Definition of Done

- This preview remains the contract
- Routes, perms, errors, and persist behaviour match this document
- I3.21–I3.35 tests pass; I4.31 and I20.22 increment strings unchanged
- Unsigned `/commercial/notifications` shows I3.35 / `0.99.0-i3.35` banner
- Visual verification of `/commercial/notifications` remains outstanding (Next/Turbopack `.next` file-lock)
- No I20.23, PG.30, I4.32, C2+, LLM, or autonomous apply

## Server / version increment

| Field | Shipped |
| --- | --- |
| `VERSION` | `0.99.0-i3.35` |
| I3 increment string | `I3.35` |
| Shell | `v0.99` · `I3.35` (keep `I4.31 · PG.29 · I20.22`) |
| Notifications eyebrow | `I3.35 · Notifications` |

Source version is **`0.99.0-i3.35`**. A still-running pre-I3.35 API process may continue to report **`0.98.0-i4.31`** until restarted. That is not a source defect.

## Rollback considerations

- No new table: rollback is code revert; PG rows keep I3.34 shape
- Unique `(tenant_id, lower(name))` is unchanged
- Deleted rows are not recoverable in this increment (hard delete, same as I20.20)
- In-memory-only Dev/Test without `EOS_DATABASE_URL` loses rename/delete on process restart unless I3.34 hydrate reloads remaining PG rows

## Security considerations

- Write gated by `notification:dispatch:email`
- Tenant isolation on every lookup
- Sanitized responses
- 404 for cross-tenant ids (no existence oracle across tenants)
- No elevation of AI autonomy; AiAgent still cannot accept/approve
- No production secrets, IdP, or NATS change

## Compatibility considerations

- I3.33 upsert-by-name remains; rename is id-based and does not replace upsert
- I3.32 last-used filter persist on export is unchanged
- I3.34 hydrate-by-id is unchanged
- I4.31 DLQ presets are untouched
- I20.22 recommend presets are untouched
- Clients that only POST upsert keep working
- Clients that apply a deleted preset get the existing 404

## Governance

| Item | Status |
| --- | --- |
| I3.35 architecture preview | Recorded (this document) |
| I3.35 implementation | Shipped `e90bbc6` · `0.99.0-i3.35` |
| I20.23 | Undefined / not authorized |
| PG.30 | Undefined / not authorized |
| I3.36 / I4.32 | Undefined / not authorized |
| C2+ | Blocked pending C1 Gate |
| AI agents / UAT / Production | Blocked |
| ADR-0006 / 0012 / 0013 | Open |
| ADR-0008 | Proposed |
| ADR-0010 | Development/Test only |
| ADR-0017 | Accepted for Development; I3 dual-write boundary reused |

## Tests

`apps/api/src/i3.35-allowlist-dual-digest-stale-audit-export-presets-rename-delete.test.ts`

## Banner

`I3.35`
