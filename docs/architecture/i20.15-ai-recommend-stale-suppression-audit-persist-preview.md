# I20.15 — Persist recommend stale suppression audit

**Increment:** I20.15  
**Server version:** `0.82.0-i20.15`  
**Autonomy:** Level 1 (Recommend) snapshot only.

## Summary

Dual-write I20.14 snooze / ack / clear audit rows when `EOS_DATABASE_URL` / `dbPool` is set, and hydrate on API startup.

## Migration

`067_i2015_ai_recommend_stale_suppression_audit.sql` — table `ai_recommend_stale_suppression_audit` (append-only, PK `id`).

## Behaviour

- Snooze/ack/clear await `persistAiRecommendStaleSuppressionAudit`
- Startup hydrate via `hydrateAiRecommendStaleSuppressionAudits`
- Last-run and last-run export still do not record a new recommend run

## Banner

`I20.15`

## Tests

`apps/api/src/i20.15-ai-recommend-stale-suppression-audit-persist.test.ts`

## Not included

- LLM providers
- Merge, email, owner-assign, or autonomous apply
- Inbox / email escalation
- Audit filters / tenant export window
