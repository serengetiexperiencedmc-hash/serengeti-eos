# I3.24 — Persist allowlist dual digest last-run

**Increment:** I3.24  
**Server version:** `0.62.0-pg24-i3.24-i4.22`

## Summary

Dual-write the I3.23 last-run stamp when `EOS_DATABASE_URL` / `dbPool` is set, and hydrate on API startup (mirrors I4.20).

## Migration

`059_i324_allowlist_dual_digest_last_run.sql` — table `notif_allowlist_dual_digest_last_run` (PK `tenant_id`).

## Behaviour

- `stampLastRun` updates in-memory store and fire-and-forget `upsertNotifAllowlistDualDigestLastRun`
- Startup hydrate via `hydrateNotifAllowlistDualDigestLastRuns`
- Status / dispatch response shape unchanged

## Banner

`I3.24`

## Tests

`apps/api/src/i3.24-allowlist-dual-digest-persist.test.ts`
