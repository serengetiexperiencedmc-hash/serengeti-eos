# I3.30 — Persist allowlist stale audit

**Increment:** I3.30  
**Server version:** `0.90.0-i3.30`

## Summary

Mirror I4.27: dual-write I3.29 snooze / ack / clear audit rows when `EOS_DATABASE_URL` / `dbPool` is set, and hydrate on API startup.

## Migration

`071_i330_allowlist_dual_digest_stale_suppression_audit.sql` — table `notif_allowlist_dual_digest_stale_suppression_audit` (append-only, PK `id`).

## Behaviour

- Snooze, ack, and digest restamp (`cleared`) append an audit row and persist it
- Startup logs `i330_allowlist_dual_digest_stale_suppression_audit_hydrate`
- Export still does not write a new audit row

## Banner

`I3.30`

## Tests

`apps/api/src/i3.30-allowlist-dual-digest-stale-suppression-audit-persist.test.ts`
