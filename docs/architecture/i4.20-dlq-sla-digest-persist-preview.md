# I4.20 — Persist DLQ SLA digest last-run to Postgres

Dual-write the I4.19 last-run stamp when `EOS_DATABASE_URL` / `dbPool` is set, and hydrate on API startup.

## Migration

`058_i420_dlq_sla_digest_last_run.sql` — table `notif_dlq_sla_digest_last_run` (PK `tenant_id`).

## Behaviour

- `stampLastRun` updates in-memory store and fire-and-forget `upsertNotifDlqSlaDigestLastRun`
- Startup hydrate via `hydrateNotifDlqSlaDigestLastRuns`
- `GET …/dlq-sla-digest-status` and dispatch responses unchanged in shape

## Banner

`I4.20`

## Tests

`apps/api/src/i4.20-dlq-sla-digest-persist.test.ts`
