# I3.13 — Suppression Bulk Lift & Import

**Increment:** I3.13  
**Server version:** `0.49.0-i4.10-i3.13`

## Summary

Bulk-lift suppressions by id/email and import from JSON items or CSV (`email,reason`).

## API

| Route | Purpose |
| --- | --- |
| POST `/v1/notifications/email/suppressions/bulk-lift` | `{ ids?, emails? }` → lifted/notFound |
| POST `/v1/notifications/email/suppressions/import` | `{ items? }` or `{ csv? }` |

## Health

`increment: I3.13`

## UI

Notifications — multi-select Lift selected; CSV import box.

## Verification

```bash
npm run test -w @sedmc/api   # i3.13-suppression-bulk.test.ts
```
