# I3.28 — Persist allowlist stale snooze/ack

**Increment:** I3.28  
**Server version:** `0.66.0-pg28-i3.28-i4.26`

## Summary

Mirror I4.25: dual-write I3.27 snooze/ack when `EOS_DATABASE_URL` / `dbPool` is set, and hydrate on API startup. Dispatching the digest deletes the row.

## Migration

`062_i328_allowlist_dual_digest_stale_suppression.sql` — table `notif_allowlist_dual_digest_stale_suppression` (PK `tenant_id`).

## Banner

`I3.28`

## Tests

`apps/api/src/i3.28-allowlist-dual-digest-stale-suppression-persist.test.ts`
